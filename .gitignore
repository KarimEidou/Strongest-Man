tools/node_modules/
tools/test/shots/
tools/package-lock.json
*.log
.DS_Store
tools/.assetcache/

# The full screenshot matrix: 602 captures, ~600 MB, regenerated by
# tools/capture/capture.mjs. Never committed — this repo IS the deployed site,
# and Git LFS is not an option because Pages serves the pointer file. The
# committed evidence is screenshots/{before,after}/ plus the two report.json
# files, written by tools/capture/contact.mjs.
screenshots/final/
screenshots/baseline/
screenshots/layout-report.json
screenshots/spec/
