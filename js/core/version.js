// Shown in the corner of the title screen. Bump MINOR for a batch of fixes or a
// new feature, PATCH for a single fix. The service worker's cache key is a
// content hash and is independent of this — see tools/gen-sw.mjs.
export const VERSION = '0.6.0';
